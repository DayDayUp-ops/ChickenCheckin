# 2.0 更新网站说明

## 一、先升级现有 D1 数据库

如果你已经为旧版创建过 D1 数据库，请打开 Cloudflare 的 D1 数据库控制台，将 `database/upgrade-to-2.0.sql` 的全部内容粘贴并执行一次。

这一步会：

- 删除旧版“最多 50 个账号”的限制；
- 清空所有账号现有的云端打卡记录；
- 保留已经注册的用户名和密码。

不要重复执行该文件。全新创建的数据库只执行 `database/schema.sql`，不要执行升级文件。

## 二、更新 Cloudflare 加密变量

打开 Cloudflare Pages 项目 → 设置 → 变量和机密，确保生产环境存在：

- `INVITE_CODE`：设置为 `yaoqingma`；
- `AUTH_PEPPER`：沿用旧值，已有账号后绝对不要修改；
- `RECOVERY_CODE`：设置为 `huifuma`。

三个变量都应选择“加密／Secret”。

## 三、更新 GitHub

打开仓库 `DayDayUp-ops/xiaoshuai-checkin-cloud`：

1. 点击 **Add file → Upload files**。
2. 将本发布包中的 `public`、`src`、`scripts`、`database`、`test` 文件夹，以及 `package.json`、`README.md`、`.gitignore`、本说明文件拖入上传区；同名文件会更新为 2.0。
3. 提交说明可填写：`更新到 2.0 并加入云同步密码找回`。
4. 点击 **Commit changes**。

Cloudflare Pages 会自动开始新的部署；构建设置仍然是：

- 构建命令：`npm run build`
- 输出目录：`dist-pages`
- 根目录：留空

## 四、发布后检查

打开 `https://xiaoshuai-checkin-cloud.pages.dev/api/health`，应显示：

```json
{"ok":true,"service":"xiaoshuai-checkin-sync"}
```

然后回到首页注册或登录。同步密码至少 6 位。忘记密码时点击云同步窗口内的“忘记密码？”，输入同步账号、恢复码 `huifuma` 和新密码。

安全提醒：任何知道同步账号和恢复码的人都能重设对应账号，请只把 `huifuma` 告诉你信任的人。
