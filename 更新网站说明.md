# 小鸡打卡 3.0 更新与更换网址说明

## 一、先在 GitHub 更新并改名

当前仓库：`DayDayUp-ops/xiaoshuai-checkin-cloud`

1. 打开仓库，进入 **Settings → General**。
2. 在 **Repository name** 中填写 `ChickenCheckin`，点击 **Rename**。
3. 返回仓库首页，确认地址变成 `https://github.com/DayDayUp-ops/ChickenCheckin`。
4. 点击 **Add file → Upload files**。
5. 上传本发布包中的 `public`、`src`、`scripts`、`database`、`test` 文件夹，以及 `package.json`、`README.md`、`.gitignore`、`更新网站说明.md`。
6. 提交说明填写 `更新小鸡打卡 3.0`，点击 **Commit changes**。

GitHub 仓库改名不会把现有 Cloudflare Pages 的 `pages.dev` 地址一起改名。

## 二、若只更新旧网站

如果暂时继续使用 `xiaoshuai-checkin-cloud.pages.dev`，GitHub 提交完成后，打开旧 Cloudflare Pages 项目的 **Deployments**，等待自动构建完成即可。

构建设置保持：

- Production branch：`main`
- Framework preset：`None`
- Build command：`npm run build`
- Build output directory：`dist-pages`
- Root directory：留空

## 三、创建新的 chickencheckin.pages.dev

Cloudflare Pages 目前不能直接修改已有的 `*.pages.dev` 子域名。请保留旧项目作为备份，再新建一个 Pages 项目：

1. 进入 Cloudflare **Workers & Pages**。
2. 点击 **Create application**。如果页面底部出现 **Continue to Pages**，先点击它，再选择 **Connect to Git**。
3. 选择 GitHub 仓库 `DayDayUp-ops/ChickenCheckin`。
4. Project name 填写 `chickencheckin`。网址最终会使用小写：`https://chickencheckin.pages.dev/`。如果名字已被占用，Cloudflare 会要求换名或生成带后缀的地址。
5. Production branch 选择 `main`。
6. Framework preset 选择 `None`。
7. Build command 填写 `npm run build`。
8. Build output directory 填写 `dist-pages`。
9. Deploy command 不填写；不要填 `npx wrangler deploy`。
10. Root directory 留空，保存并部署。

## 四、让新项目继续使用原有账号和数据

新项目创建后，进入新项目的 **Settings → Bindings**：

1. 添加 D1 database binding。
2. Variable name 必须填写 `DB`。
3. Database 选择旧网站正在使用的同一个 D1 数据库。

不要新建数据库，也不要再次执行 `schema.sql` 或 `upgrade-to-2.0.sql`，否则不会自动带上原有账号和记录。

然后进入 **Settings → Variables and Secrets**，为 Production 添加三个 Secret：

- `INVITE_CODE`：使用旧项目相同的值（当前设置应为 `yaoqingma`）。
- `RECOVERY_CODE`：使用旧项目相同的值（当前设置应为 `huifuma`）。
- `AUTH_PEPPER`：必须使用旧项目原来的完整值，不能重新随机生成，否则旧密码将无法验证。

保存后进入 **Deployments**，对最新部署点击 **Retry deployment**，或在 GitHub 再提交一次小改动触发重新部署。

## 五、发布后检查

依次检查：

1. 打开 `https://chickencheckin.pages.dev/`，确认页面显示“小鸡打卡”和新 Logo。
2. 打开 `https://chickencheckin.pages.dev/api/health`，应显示类似：

```json
{"ok":true,"service":"chicken-checkin-sync"}
```

3. 用原来的同步账号和密码登录，确认原有云端记录存在。
4. 修改一条不重要的设置，在另一台设备登录相同账号，确认能够自动同步。

确认新网址完全正常后，可以继续保留旧项目用于回退。删除旧 Pages 项目不会删除独立的 D1 数据库，但不要在 D1 控制台删除数据库。
